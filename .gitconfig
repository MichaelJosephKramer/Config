[alias]
  alias  = !git config -l | grep alias | cut -c 7- | sort
  hist   = log --pretty=format:\"%Cred%h%Creset %ad | %s%d %C(bold blue)[%an]%Creset %Cgreen(%cr)%Creset\" --graph --date=short
  last   = cat-file commit HEAD
  rmall  = !git ls-files --deleted | xargs git rm
  this   = !git init && git add . && git commit -m 'Initial commit'
  sha    = !git rev-parse HEAD | tr -d \"\n\" | pbcopy
  br     = !git name-rev HEAD --name-only | tr -d \"\n\" | pbcopy
  flush  = !git branch --merged master | grep -v master | xargs git branch -d && git remote prune origin
  flushd = !git branch --merged develop | grep -v develop | xargs git branch -d && git remote prune origin
  me     = log --pretty=format:\"%Cred%h%Creset %ad | %s%d %C(bold blue)[%an]%Creset %Cgreen(%cr)%Creset\" --date=short --author=michael@sagehou.se
[color]
  ui = true
[user]
  name  = Michael Joseph Kramer
  email = michael@sage.house
[core]
  editor       = /usr/local/bin/vim
  pager        = less -F -X
  excludesfile = ~/.gitignore-global
[merge]
  tool       = p4merge
  keepBackup = false
[mergetool "p4merge"]
  cmd             = p4merge "$BASE" "$LOCAL" "$REMOTE" "$MERGED"
  trustExitCode   = false
  keepTemporaries = false
  keepBackup      = false
[difftool]
  external = p4diff
[mergetool]
  keepBackup = false
[heroku]
  account = sagehouse
